# シリーズ価格推移グラフ

最終更新: 2026-09-04  
主要実装:
- PR #90 / merge commit `7ac8ce17bd8cff54fbc50e57acaada1d7764130c`
- PR #91 / merge commit `cca82b51a847207d4bb591cde66d091f178152d6`
- PR #92 / merge commit `98288a8e8b6928d31cfb9eeca05a21c797e97f94`

## 目的

商品詳細ページの既存「価格推移」グラフの下に `シリーズ` ボタンを表示し、押すと、その商品が属するシリーズの登録済み各作品の販売価格推移を1枚のグラフへ重ねて表示する。

今回の実装範囲はこの機能だけ。シリーズ平均・中央値・合計価格、シリーズ編集UI、DBのSeriesモデル、Viewer側表示は追加していない。

## シリーズ定義

2026-09-04に作成した成人向けゲームシリーズ調査表を元に、173シリーズ・1,135タイトルを静的カタログ化した。

ファイル:

- `data/series-catalog-01.json`
- `data/series-catalog-02.json`
- `data/series-catalog-03.json`
- `data/series-catalog-04.json`
- `data/series-catalog-05.json`
- `data/series-catalog-06.json`

DBへシリーズ情報は保存しない。シリーズの正規定義は現時点ではこの静的カタログ。

## 商品タイトルのシリーズ判定

`lib/series-catalog.ts` が担当する。

基本方針:

1. 商品タイトルの状態表記を `splitProductTitleCondition()` で除去する。
2. 駿河屋が先頭へ付ける `WindowsXP/Vista/7/8 DVD ソフト` 等の機種・媒体カテゴリ表記を除去する。
3. NFKC正規化・小文字化を行う。
4. 空白とUnicode句読点を除去する。
5. `+`、`×`、`†` などタイトル識別に使う記号は残す。
6. カタログ側タイトルを長い順に照合する。
7. 正規化後の完全一致または前方一致だけを採用する。

部分包含一致は使わない。別シリーズへの誤分類を避けるため。

長いタイトルを優先することで、たとえば `ToHeart2 ダンジョントラベラーズ` を `ToHeart2` 系ではなくダンジョントラベラーズとして判定する。

## 同一作品の複数登録

同じカタログ作品に複数の駿河屋商品が対応する場合:

- 通常品が存在すれば通常品を優先する。
- ランクB/状態違い商品は通常品がある場合はシリーズ線から除外する。
- 通常品が存在しない場合は状態違い商品も利用可能とする。

## 商品詳細ページ

`app/products/[id]/page.tsx` で現在の商品からシリーズを判定する。

シリーズが見つかった場合のみ:

1. 登録商品のID・タイトル・状態を取得する。
2. `buildSeriesProductGroups()` でシリーズ内の作品へ分類する。
3. そのシリーズに必要な `PriceHistory` のみ取得する。
4. `SeriesPriceChart` へ渡す。

既存の `PriceChart` は変更しない。その直下にシリーズUIを追加する。

## SeriesPriceChart

`components/SeriesPriceChart.tsx`

初期状態では `シリーズ` ボタンだけを表示する。

押下後:

- シリーズ名
- 価格履歴が存在する作品数 / カタログ定義作品数
- 同シリーズ各作品の販売価格推移
- 各作品の最新販売価格を含む凡例

を表示する。各作品を1本の折れ線として同じ時間軸へ重ねる。

### 価格差が大きい場合の可読性

PR #92で改善。

- 縦軸は `自動 / 通常 / 対数` を切替可能。
- 初期値は `自動`。
- シリーズ内の最大価格 / 最小価格が8倍以上で、すべて正の価格なら自動的に対数目盛を使う。
- これにより、Rance35th Anniversary Boxのような高額商品が存在しても千円台の各作品の値動きが最下部へ潰れにくい。
- `通常` を選べば従来どおり金額差を線形表示できる。
- 凡例の作品名またはグラフ線へホバーすると対象線を強調し、他の線を薄くする。
- クリックすると強調状態を固定し、再クリックで解除する。

対数目盛は価格そのものを書き換えるものではない。縦方向の間隔だけを対数化し、凡例の現在価格は実額を表示する。

## 現時点で未実装

- Viewerでのシリーズ価格グラフ
- シリーズ平均価格
- シリーズ中央値
- シリーズ全取得価格
- シリーズ所属の編集画面
- Series / SeriesProduct Prismaモデル
- シリーズ単独ページ

これらを追加する場合も、まず今回の「商品詳細からシリーズ全作品の価格推移を見る」という導線を壊さないこと。

## 回帰テスト

- `lib/series-catalog.test.ts`
- `lib/series-price-chart-ui.test.ts`

PR #90、#91、#92で `npm test`、`npm run typecheck`、`npm run build` のCI成功を確認している。
