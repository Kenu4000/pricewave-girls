# 他PC・スマホから同じ価格データを見る

駿河屋へのアクセスと価格取得はメインPCだけで行い、SQLite DBもメインPCにだけ置きます。
他PCやスマホは、メインPCで起動しているNext.jsへブラウザで接続して同じデータを参照します。

## 構成

- メインPC
  - Edge拡張機能で駿河屋を取得
  - `prisma/dev.db` に価格履歴を保存
  - Webアプリをポート3000で公開
- 他PC・スマホ
  - DBや拡張機能は不要
  - ブラウザからメインPCのIPアドレスへアクセス

## 起動

通常どおり `start-tracker.cmd` を起動してください。
起動時に次のようなURL候補が表示されます。

```text
メインPC: http://localhost:3000
他PC・スマホ（同じLAN / 接続可能なVPN）:
  http://192.168.1.20:3000
```

スマホや別PCをメインPCと同じWi-Fi/LANへ接続し、表示された `http://192.168.x.x:3000` をブラウザで開きます。

Windowsファイアウォールの確認が出た場合は、信頼できるプライベートネットワークだけ許可してください。

## IPアドレスが変わった場合

DHCPでメインPCのIPv4アドレスが変わることがあります。
その場合は `start-tracker.cmd` を起動し直すか、次を実行すると現在のURL候補を再表示できます。

```bash
npm run share:url
```

## 外出先から見る場合

この変更だけで直接インターネットへポート3000を公開することは想定していません。
外出先から参照する場合は、メインPCと閲覧端末を同じVPNへ参加させ、そのVPN上のIPアドレスでアクセスする構成を使ってください。

ルーターのポート開放でNext.jsを直接インターネットへ公開する運用は、認証を付けていない現状では使用しないでください。
